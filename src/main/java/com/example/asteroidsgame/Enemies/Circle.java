package com.example.asteroidsgame.Enemies;

import com.example.asteroidsgame.Character;
import javafx.scene.shape.Polygon;

public class Circle extends Character {
    public Circle(Polygon polygon, int x, int y) {
        super(polygon, x, y);
    }
}
